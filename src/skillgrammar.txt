skillgraph
  = BOM? line*

BOM
  = '\uFEFF'

line
  = node / comment / blankline

node
  = name '.' 
  / name ':' name number (',' name number)* '.'

comment
  = '#' .* '\n'

blankline
  = '\n'

ws
  = ' '*

name
  = [a-z]+

number
   = [0-9]+
